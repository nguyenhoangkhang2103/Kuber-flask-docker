apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-ingresse-test
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: web.globalproxy.online
    http:
      paths:
      - backend:
          serviceName: app
          servicePort: 80
        path: /
